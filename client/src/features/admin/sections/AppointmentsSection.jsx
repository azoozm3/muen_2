import { AppointmentRequestsSection } from "@/features/admin/sections/requests/RequestSections";

export function AppointmentsSection({ data }) {
  return <AppointmentRequestsSection data={data} />;
}
