export const EMERGENCY_TERMINAL_STATUSES = new Set(["completed", "cancelled"]);
export const NURSE_TERMINAL_STATUSES = new Set(["completed", "cancelled", "refunded"]);
export const APPOINTMENT_TERMINAL_STATUSES = new Set(["completed", "cancelled", "rejected"]);
export const VOLUNTEER_TERMINAL_STATUSES = new Set(["completed", "cancelled"]);
